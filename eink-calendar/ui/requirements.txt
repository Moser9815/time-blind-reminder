google-auth>=2.30
google-auth-oauthlib>=1.2
google-api-python-client>=2.130
playwright>=1.45
pillow>=10.4
numpy>=2.0
# Browser cookie auth — only needed when apps_script_auth = "browser"
# (workaround for restricted-access Apps Script web apps that don't accept
# Bearer tokens. Reads cookies from the user's Chrome profile via Keychain.)
browser-cookie3>=0.20
requests>=2.31
